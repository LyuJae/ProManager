<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ryu.promanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TextAct" android:label="프로젝트 1번" ></activity>
        <activity android:name=".TextAct2" android:label="프로젝트 2번" ></activity>
        <activity android:name=".TextAct3" android:label="프로젝트 3번" ></activity>
        <activity android:name=".TextAct4" android:label="프로젝트 4번" ></activity>
    </application>

</manifest>